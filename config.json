
{
  "paths": {
    "title": "paths",
    "description": "JavaScript expression which returns a sequence of string",
    "type": "string",
    "default": ""
  },
  "json": {
    "title": "paths.json",
    "description": "Path to an JSON file which represents paths (as a sequence of string)",
    "type": "string",
    "default": ""
  },
  "js": {
    "title": "paths.js",
    "description": "Path to a [node package](https://nodejs.org/api/modules.html) which returns (exports) a sequence of paths",
    "type": "string",
    "default": ""
  }
}
